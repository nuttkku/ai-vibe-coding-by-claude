Run the project checks and report back in Thai.

Scope (optional): $ARGUMENTS

1. Run `git status` and list changed files.
2. Run the test suite (`npm test`). If tests fail, show the failing test names and the key error lines.
3. Look at `git diff` for obvious problems: leftover console.log, secrets or passwords, commented-out code, missing error handling.
4. Do NOT modify any files. End with a short verdict: "พร้อม commit" or "ยังไม่พร้อม" plus the reasons.
