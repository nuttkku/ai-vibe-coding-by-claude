# CLAUDE.md — คำแนะนำสำหรับ Claude ใน repo นี้

Repo นี้คือ **สื่อการสอน** หลักสูตร "AI Vibe Coding ด้วย Claude" 5 วัน ไม่ใช่ตัวแอป

## ภาษาและน้ำเสียง
- เนื้อหาบทเรียนเขียนเป็น **ภาษาไทย** ศัพท์เทคนิคคงเป็นภาษาอังกฤษ (เช่น Commit, Docker Compose, Prompt)
- ผู้เรียนเป็นผู้เริ่มต้นถึงระดับกลาง อธิบายให้ทำตามได้ทีละขั้น พร้อมคำสั่งที่ copy ไปรันได้
- โค้ด, comment ในโค้ด และชื่อไฟล์ใช้ภาษาอังกฤษ

## โครงสร้าง
- `day-N-*/README.md` — บทเรียนรายวัน: เป้าหมาย → ตารางเวลา → ขั้นตอน → Prompt ตัวอย่าง → Checklist → Troubleshooting → อ้างอิง
- `day-N-*/examples/` — ไฟล์ตัวอย่างที่รันได้จริง
- `templates/` — แม่แบบที่ผู้เรียนคัดลอกไปใช้ในโปรเจกต์ของตัวเอง
- `prompts/` — คลัง Prompt
- `guides/` — คู่มือเสริมที่ใช้ข้ามหลายวัน
- `scripts/` — สคริปต์สำหรับผู้เรียน (ต้องเป็นแบบอ่านอย่างเดียว; `.ps1` ต้องรันได้ทั้ง Windows PowerShell 5.1 และ 7 และใช้ข้อความภาษาอังกฤษเพื่อเลี่ยงปัญหา encoding)

## กติกาเมื่อแก้ไข
- ทุกครั้งที่อ้างอิงเอกสารภายนอกใหม่ ให้เพิ่มลงใน `CREDITS.md` ด้วย
- เนื้อหาบทเรียน = CC BY-NC-SA 4.0 ([LICENSE-EDU.md](LICENSE-EDU.md)), โค้ด/config/template = MIT ([LICENSE](LICENSE)) — ถ้าเพิ่มไฟล์ประเภทใหม่ให้ระบุว่าอยู่ใต้ license ไหนใน `README.md`
- ห้ามวาง workflow ตัวอย่างไว้ใน `.github/workflows/` ของ repo นี้ (จะถูกรันกับ repo การสอน) — เก็บไว้ใน `day-4-build-deploy/examples/`
- เวอร์ชันเครื่องมือเปลี่ยนเร็ว: ระบุแบบ "LTS ล่าสุด" หรือให้ผู้เรียนตรวจด้วยคำสั่ง แทนการ hard-code เลขเวอร์ชันในเนื้อหา (ยกเว้นใน config ที่ต้อง pin)
- ตัวอย่าง Security Scan ต้องสแกนเฉพาะแอป/เครื่องของผู้เรียนเองเท่านั้น และย้ำเรื่องนี้ในบทเรียน
